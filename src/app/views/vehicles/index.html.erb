<h1>Car Listing</h1>

<table class="table table-hover table-striped text-center">
  <thead>
    <tr>
      <th scope="col">Brand</th>
      <th scope="col">Model</th>
      <th scope="col">Body Type</th>
      <th scope="col">Door Count</th>
      <th scope="col">Seat Count</th>
      <th scope="col">Available?</th>
      <th scope="col"></th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td scope="row"><%= vehicle.brand %></td>
        <td scope="row"><%= vehicle.model %></td>
        <td scope="row"><%= vehicle.body_type %></td>
        <td scope="row"><%= vehicle.door_count %></td>
        <td scope="row"><%= vehicle.seat_count %></td>
        <% if vehicle.available %>
          <td scope="row">✅</td>
        <% else %>
          <td scope="row">❌</td>
        <% end %>
        <td scope="row"><a class="btn btn-primary btn-sm" href="<%= vehicle_path(vehicle.id) %>" role="button">Show</a></td>
        <td scope="row"><button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#ConfirmDelete">Delete</button></td>
      </tr>
        <!-- Modal -->
      <div class="modal fade" id="ConfirmDelete" tabindex="-1" role="dialog" aria-labelledby="ConfirmDeleteTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="ConfirmDeleteTitle">Confirm</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              Are you sure?
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <%= link_to 'Delete', delete_vehicle_path(vehicle.id), method: :delete, class: 'btn btn-danger' %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </tbody>
</table>


